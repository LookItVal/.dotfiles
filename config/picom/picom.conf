backend = "glx";
corner-radius = 10;

rules: ({
  match = "(class_g = 'URxvt' || class_g = 'Alacritty')"
          " && !focused";
  opacity = 0.8;
},
{
  match = "(class_g = 'URxvt' || class_g = 'i3bar' || class_g = 'Rofi')";
  corner-radius = 0;
});